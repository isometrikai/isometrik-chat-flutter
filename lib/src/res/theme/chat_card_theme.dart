import 'package:flutter/material.dart';

class IsmChatCardTheme {
  const IsmChatCardTheme(
      {this.backgroundColor, this.textColor, this.iconColor});

  final Color? backgroundColor;
  final Color? textColor;
  final Color? iconColor;
}
